(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.MangaExtensionBackground=api;if(typeof chrome!=='undefined'&&chrome.runtime&&chrome.storage)api.installChromeHandlers(chrome);})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';
const KEYS={rules:'mangaSiteRulesV1',origins:'mangaRegisteredOriginsV1',pending:'mangaPendingDraftsV1',exportDrafts:'mangaExportDraftsV1'},SITE_SCRIPT_PREFIX='testcode-manga-site-';
function makeMemoryStore(initial={}){const data={...initial};return{async get(k){return data[k]},async set(k,v){data[k]=v}}}function chromeStore(c){return{async get(k){return(await c.storage.local.get(k))[k]},async set(k,v){await c.storage.local.set({[k]:v})}}}
function makeQueue(store,key=KEYS.pending){return{async list(){const v=await store.get(key);return Array.isArray(v)?v:[]},async enqueue(draft){const a=await this.list();const id='q_'+Date.now().toString(36)+Math.random().toString(36).slice(2,8);a.push({id,draft,queuedAt:Date.now()});await store.set(key,a);return id},async ack(id){await store.set(key,(await this.list()).filter(x=>x.id!==id))}}}
function makeSerializedFlusher(work){const m=new Map();return function(k){if(m.has(k))return m.get(k);let p;try{p=Promise.resolve(work(k))}catch(e){p=Promise.reject(e)}p=p.finally(()=>{if(m.get(k)===p)m.delete(k)});m.set(k,p);return p}}
function summarizeRuleFields(rule,total){return{configured:rule&&rule.fields?Object.keys(rule.fields).filter(k=>rule.fields[k]).length:0,total:Number(total)||0}}function deliveryStateFromResult(r){return r&&r.status==='added'?'synced':r&&r.status==='duplicate'?'duplicate':r&&r.status==='locked'?'locked':'waiting'}async function flushPending(){return{delivered:0,lastResult:null,diagnostics:[]}}
function scriptId(origin){let hash=2166136261;for(const ch of String(origin)){hash^=ch.charCodeAt(0);hash=Math.imul(hash,16777619)}return SITE_SCRIPT_PREFIX+(hash>>>0).toString(36)}
function originMatch(origin){try{const u=new URL(origin);return u.origin+'/*'}catch(_){return null}}
async function syncRegisteredContentScripts(chromeApi,store){if(!chromeApi.scripting||typeof chromeApi.scripting.registerContentScripts!=='function')return;const origins=Array.isArray(await store.get(KEYS.origins))?await store.get(KEYS.origins):[],desired=[];for(const origin of origins){const match=originMatch(origin);if(!match)continue;let granted=false;try{granted=await chromeApi.permissions.contains({origins:[match]})}catch(_){}if(granted)desired.push({id:scriptId(origin),matches:[match],js:['content/rule-locator.js','content/extractor.js','content/element-picker.js','content/site-toolbar.js'],runAt:'document_idle',persistAcrossSessions:true})}let existing=[];try{existing=await chromeApi.scripting.getRegisteredContentScripts()}catch(_){}const managed=existing.filter(x=>String(x.id||'').startsWith(SITE_SCRIPT_PREFIX));if(managed.length)try{await chromeApi.scripting.unregisterContentScripts({ids:managed.map(x=>x.id)})}catch(_){}if(desired.length)await chromeApi.scripting.registerContentScripts(desired)}
async function importBatchMain(batch){const normalize=v=>{try{const u=new URL(String(v||'').trim());if(!/^https?:$/.test(u.protocol))return null;u.hash='';return u.href}catch(_){return null}};if(!batch||batch.version!==1||!Array.isArray(batch.items))throw new Error('JSON形式が正しくありません。');const key='mangaReaderSavedItems',previous=localStorage.getItem(key);let items;try{items=JSON.parse(previous||'[]');if(!Array.isArray(items))items=[]}catch(_){items=[]}let added=0,duplicates=0;for(const raw of batch.items){if(!raw||typeof raw!=='object')throw new Error('漫画データが不正です。');const pages=Array.isArray(raw.pages)?raw.pages.map(normalize).filter(Boolean):[],url=normalize(raw.url),sourcePageUrl=normalize(raw.sourcePageUrl),primary=pages[0]||url;if(!primary)throw new Error('画像URLがありません。');const duplicate=items.some(x=>{const xp=Array.isArray(x.pages)&&x.pages.length?normalize(x.pages[0]):normalize(x.url);return(primary&&xp===primary)||(sourcePageUrl&&normalize(x.sourcePageUrl)===sourcePageUrl)});if(duplicate){duplicates++;continue;}const item={id:'i'+Date.now().toString(36)+Math.random().toString(36).slice(2,7),title:String(raw.title||'無題').slice(0,1000),author:String(raw.author||'').slice(0,1000),tags:Array.isArray(raw.tags)?raw.tags.map(String).slice(0,100):[],folderId:null,favorite:false,splitSpreads:false,addedAt:Date.now(),sourcePageUrl:sourcePageUrl||null};if(raw.series)item.series=String(raw.series).slice(0,1000);if(raw.volume!=null){const m=String(raw.volume).match(/\d+(?:\.\d+)?/);if(m)item.volume=Number(m[0])}if(raw.source)item.source=String(raw.source).slice(0,1000);if(pages.length>=2)item.pages=pages;else item.url=pages[0]||url;items.unshift(item);added++;}if(!added)return{added:0,duplicates};localStorage.setItem(key,JSON.stringify(items));try{if(!globalThis.MangaVault||typeof MangaVault.savePayload!=='function'||!globalThis.MangaVaultPayload||typeof MangaVaultPayload.buildFromLocalStorage!=='function')throw new Error('暗号化同期の準備ができていません。');await MangaVault.savePayload(MangaVaultPayload.buildFromLocalStorage());}catch(e){if(previous===null)localStorage.removeItem(key);else localStorage.setItem(key,previous);throw e;}return{added,duplicates};}
function installChromeHandlers(chromeApi){const store=chromeStore(chromeApi),exportQueue=makeQueue(store,KEYS.exportDrafts),sync=()=>syncRegisteredContentScripts(chromeApi,store).catch(()=>{});if(chromeApi.runtime.onInstalled)chromeApi.runtime.onInstalled.addListener(sync);if(chromeApi.runtime.onStartup)chromeApi.runtime.onStartup.addListener(sync);sync();chromeApi.runtime.onMessage.addListener((message,sender,sendResponse)=>{if(!message||!message.type){sendResponse({ok:false});return false}(async()=>{if(message.type==='GET_RULES'){const v=await store.get(KEYS.rules);return{ok:true,rules:Array.isArray(v)?v:[]}}if(message.type==='SAVE_RULE'){const all=Array.isArray(await store.get(KEYS.rules))?await store.get(KEYS.rules):[],next={...message.rule,updatedAt:Date.now()},i=all.findIndex(r=>r.id===next.id);if(i>=0)all[i]=next;else all.push(next);await store.set(KEYS.rules,all);return{ok:true,rule:next}}if(message.type==='QUEUE_DRAFT'){const id=await exportQueue.enqueue(message.draft);return{ok:true,id,count:(await exportQueue.list()).length}}if(message.type==='GET_EXPORT_STATUS')return{ok:true,count:(await exportQueue.list()).length};if(message.type==='EXPORT_DRAFTS_JSON'){const rows=await exportQueue.list();if(!rows.length)return{ok:false,error:'書き出し待ちの漫画がありません。'};const payload={version:1,exportedAt:new Date().toISOString(),items:rows.map(x=>x.draft)};const text=JSON.stringify(payload,null,2),url='data:application/json;charset=utf-8,'+encodeURIComponent(text),stamp=new Date().toISOString().replace(/[:.]/g,'-');await chromeApi.downloads.download({url,filename:`testcode-manga-${stamp}.json`,saveAs:true});await store.set(KEYS.exportDrafts,[]);return{ok:true,count:rows.length};}if(message.type==='IMPORT_JSON_BATCH'){if(!sender.tab||typeof sender.tab.id!=='number')return{ok:false,error:'readerタブを特定できません。'};const result=await chromeApi.scripting.executeScript({target:{tabId:sender.tab.id},world:'MAIN',func:importBatchMain,args:[message.batch]});const value=result&&result[0]&&result[0].result;if(!value)return{ok:false,error:'取り込み処理の結果を取得できません。'};return{ok:true,...value};}if(message.type==='GET_SITE_STATUS'){const origins=await store.get(KEYS.origins),rules=await store.get(KEYS.rules);return{ok:true,registered:Array.isArray(origins)&&origins.includes(message.origin),rules:(Array.isArray(rules)?rules:[]).filter(r=>r.origin===message.origin)}}if(message.type==='REGISTER_SITE'){const origin=message.origin,match=originMatch(origin);if(!match)return{ok:false,error:'invalid-origin'};const granted=await chromeApi.permissions.contains({origins:[match]});if(!granted)return{ok:false,error:'permission-not-granted'};const origins=Array.isArray(await store.get(KEYS.origins))?await store.get(KEYS.origins):[];if(!origins.includes(origin)){origins.push(origin);await store.set(KEYS.origins,origins)}await syncRegisteredContentScripts(chromeApi,store);if(message.tabId)try{await chromeApi.scripting.executeScript({target:{tabId:message.tabId},files:['content/rule-locator.js','content/extractor.js','content/element-picker.js','content/site-toolbar.js']})}catch(_){}return{ok:true}}return{ok:false,error:'unknown-message'};})().then(sendResponse).catch(e=>sendResponse({ok:false,error:String(e&&e.message||e)}));return true});}
return{KEYS,makeMemoryStore,makeQueue,makeSerializedFlusher,summarizeRuleFields,deliveryStateFromResult,installChromeHandlers,flushPending,importBatchMain,scriptId,syncRegisteredContentScripts};});