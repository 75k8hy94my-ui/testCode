(() => { 'use strict'; const page=document.getElementById('videoPlayerPage'); const id=new URLSearchParams(location.search).get('id')||''; const read=(key,fallback)=>{try{const value=JSON.parse(localStorage.getItem(key)||'null');return value==null?fallback:value}catch(_){return fallback}}; const base=read('mangaReaderVideos',[]).find(item=>String(item.id)===id); const meta=read('mangaReaderVideoMeta',{})[id]||{}; if(!base){page.innerHTML='<section class="profileContent"><h2>動画が見つかりません</h2><a class="glassBtn" href="video.html">動画一覧へ戻る</a></section>';return} const title=meta.title||base.title||[base.a,base.b].filter(Boolean).join(' / ')||'動画'; const heading=document.createElement('h2');heading.textContent=title; const frame=document.createElement('div');frame.className='videoPlayerFrame'; if(base.a&&base.b){const iframe=document.createElement('iframe');iframe.src='https://www.'+base.a+'.com/embed/'+base.b;iframe.title=title;iframe.allowFullscreen=true;frame.append(iframe)}else{const link=document.createElement('a');link.className='glassBtn';link.href=base.url||'#';link.target='_blank';link.rel='noopener';link.textContent='元ページを開く';frame.append(link)} const back=document.createElement('a');back.className='glassBtn videoBack';back.href='video.html';back.textContent='動画一覧へ戻る';page.replaceChildren(heading,frame,back) })();
