# 暗号化クラウド同期

このアプリはSupabaseのメールアカウントで端末間同期します。クラウドへ送る前に、読書リスト・進捗・リンク・動画・目次をブラウザ内で暗号化します。Supabaseに保存されるのは暗号文だけです。

## 最初の設定

1. Supabase Dashboardでこのプロジェクトの **SQL Editor** を開きます。
2. `supabase-schema.sql` の内容をすべて貼り付けて **Run** を押します。
3. アプリをHTTPSで公開した後、Supabase Dashboardの **Authentication > URL Configuration** で、`Site URL` と `Redirect URLs` にアプリのURLを追加します。
4. アプリの「クラウド同期」を開き、メールアドレスとログイン用パスワードを入力してアカウントを作成します。
5. メール確認後にログインし、ログイン用とは別の保管庫パスフレーズを入力して「新しい保管庫を作成」を押します。
6. 表示される復旧キーを、パスワード管理アプリなどクラウドとは別の安全な場所に保管します。
7. 「この端末の内容を保存」を押します。

別の端末では、同じアカウントでログインすると保管庫の操作が表示されます。保管庫パスフレーズまたは復旧キーを入力して「クラウドから読み込む」を押します。「新しい保管庫を作成」は最初の一度だけです。

## 暗号方式

- データ本体: AES-256-GCM（暗号化と改ざん検知）
- 保管庫鍵: 暗号学的乱数で生成した256ビット鍵
- パスフレーズによる鍵保護: PBKDF2-HMAC-SHA-256、600,000反復、ランダム16バイトsalt
- 復旧キー: ランダム256ビット値。保管庫鍵を別経路で保護します。

保管庫の復号鍵、パスフレーズ、復旧キーはアプリやSupabaseへ保存しません。復旧キーとパスフレーズを両方失うと、内容は復元できません。

## 守れるもの・守れないもの

Supabaseのデータベース内容が漏えいしても、保管庫パスフレーズまたは復旧キーなしには保存内容を読めません。Supabase認証は「誰がその暗号文を読書きできるか」を制限し、暗号化は「保存先が漏れても内容を読めなくする」役割を担います。

ログアウト時には、端末に残るログイン用セッションと保管庫の同期履歴を削除します。クラウド上の暗号文を削除・上書きされる危険に備え、定期的なJSON書き出しも推奨します。
