# 暗号化クラウド同期

このアプリはSupabaseのメールアカウントで端末間同期します。クラウドへ送る前に、読書リスト・進捗・リンク・動画・目次・作品ごとの検出済みページ数をブラウザ内で暗号化します。Supabaseに保存されるのは暗号文だけです。

## 最初の設定

1. Supabase Dashboardでこのプロジェクトの **SQL Editor** を開きます。
2. `supabase-schema.sql` の内容をすべて貼り付けて **Run** を押します。
3. アプリをHTTPSで公開した後、Supabase Dashboardの **Authentication > URL Configuration** で、`Site URL` と `Redirect URLs` にアプリのURLを追加します。公開URLの入口は `index.html`（ログイン画面）です。
4. `index.html` でメールアドレスとログイン用パスワードを入力してアカウントを作成します。
5. メール確認後にログインし、ログイン用とは別の保管庫パスフレーズを入力します。既存の保管庫は自動で復号・読み込み、新しいアカウントでは空の保管庫を自動で作成します。
6. 新しい保管庫で表示される復旧キーを、パスワード管理アプリなどクラウドとは別の安全な場所に保管します。

別の端末では、同じアカウントでログインして保管庫パスフレーズまたは復旧キーを入力します。クラウドの内容は自動で読み込まれ、以後の追加・編集・読書位置などの変更も自動で暗号化同期されます。

## 暗号方式

- データ本体: AES-256-GCM（暗号化と改ざん検知）
- 保管庫鍵: 暗号学的乱数で生成した256ビット鍵
- パスフレーズによる鍵保護: PBKDF2-HMAC-SHA-256、600,000反復、ランダム16バイトsalt
- 復旧キー: ランダム256ビット値。保管庫鍵を別経路で保護します。

保管庫の復号鍵、パスフレーズ、復旧キーはSupabaseへ保存しません。パスフレーズは保存せず、復号鍵だけを自動同期のためにブラウザのタブを閉じるまでの一時領域に保持します。復旧キーとパスフレーズを両方失うと、内容は復元できません。

## 守れるもの・守れないもの

Supabaseのデータベース内容が漏えいしても、保管庫パスフレーズまたは復旧キーなしには保存内容を読めません。Supabase認証は「誰がその暗号文を読書きできるか」を制限し、暗号化は「保存先が漏れても内容を読めなくする」役割を担います。

ログアウト時には、端末に残るログイン用セッション・保管庫の同期履歴・保存済みURLなどの読書データを削除します。クラウド上の暗号文を削除・上書きされる危険に備え、定期的なJSON書き出しも推奨します。

## ページ数の一括読み込み

保管庫の「一括読み込み」は、保存済みURLのうちページ数が未検出の作品を順に調べ、結果を通常の同期データとして保存します。1作品につき最大6ページを並列に確認しますが、複数作品は同時に読まず、サイトへの負荷を抑えます。

画像取得がタイムアウトした場合は、確認できた最後のページ数を保持して「途中から再試行」の対象にします。再試行では既に確認済みのページを読み直さず、次のページから続けます。ファイルが存在しないことを確認できた場合だけ、その作品のページ数を確定します。
